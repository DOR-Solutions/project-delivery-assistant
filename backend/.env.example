# Backend configuration
DATABASE_URL=sqlite:///./maxai.db
CORS_ORIGINS=http://localhost:5173
# Optional: enable live AI (document analysis + chat). Without it, MAX falls back to offline extraction.
MAXAI_ANTHROPIC_KEY=
MAXAI_MODEL=claude-sonnet-4-20250514
