PORT=5000
# Change this in production so existing login tokens cannot be forged.
JWT_SECRET=replace-with-a-long-random-secret
# DATA_DIR can point to a writable directory for JSON persistence.
# DATA_DIR=./data
